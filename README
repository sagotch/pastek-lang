= pastek =

A powerful lightweight markup language

This is version 20140210.

== General notes ==

- Spaces at the beginning of a line are meaningless and will be skiped.

== Syntax ==

Title             = words = (closing '=' are optionnal)
List              # words
                    -- words
                    -- words
                       --- words
                  # words

Image             [[ url || alt plain txt ]]

Link              [[ url << displayed content ]]
                  [[ url ]]

Bold              **words**
Italic            //words//
Underline         __words__
Strike            ~~words~~
Inline code       ``words``
Inline source     {{words}}
Inline Math       $$words$$

Subscript         _i (one sign)
                  _{index} (several signs)
Superscript       ^i (one sign)
                  ^{index} (several signs)

HTML entitie      &#60;
                  &isin;

Greek letter      &a

Table             |  head1  |  head2  |  head3  | (optionnal
                  +-----------------------------+  pair)
                  | cell1.1 | cell1.2 | cell1.3 |
                  | cell2.1 | cell2.2 | cell2.3 |
		  (pipes, plus and minus do not have to match)

Code block        ```
                  let triple_backquoted =
                    "Code block"
                  ```

External command  %%%dot
                  graph graphname {
                      a -- b -- c;
                      b -- d;
                  }
                  %%%

Source block      {{{<span style="color:blue">Inline source code.</span>}}}
                  {{{
                  <p style="color:red">
                  Introduce source code in your document.
                  </p>
                  }}}

Math block        $$$
                  &forall; a, b, c &isin; [0;9] | a < b,
                                                  a^2 + b^2 = c^2
                  ...
                  $$$

== Configuration ==

Configuration is written in TOML. It can be provided by a configuration file, 
a special header in a document (written inside %{ and }% tags), or by a 
combination of both. Each rendering module could expect different 
configuration, so look at their documentation for details.

Here is an example of configuration informations included at the beginning of a
document rendered by ``Render_html`` module.

%{
[css]
    
    [css.default]
    url = "https://raw.github.com/sagotch/pastek/res/pastek_default.css"
    inline = false # default behavior so you may remove this line
                   # produce lighter documents
    [css.custom]
    url = "/home/foo/bar.css"
    inline = true # inlining allow to copy content of the file in
                  # document header

[cmd]
   [cmd.dot]
   cmd = "dot -Tsvg - -"
   [cmd.hopt]
   cmd = "~/html_tool/hopt"
%}